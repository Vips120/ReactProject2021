export const randomCoin = (arr) => {
    let idx = Math.floor(Math.random() * arr.length);
    return arr[idx];
};

/***
 * RandomCOlorPalette
 */

export const randomColor = (arr) => {
    let idx = Math.floor(Math.random() * arr.length);
    return arr[idx];
};